import DateUtils from "../utils/DateUtils";
import SyncStrategy from "./SyncStrategy";

export default class MergeStrategy extends SyncStrategy {
    
    public async apply(data : any, data2 : any) : Promise<any> {


    }

}